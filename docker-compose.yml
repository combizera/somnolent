services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: somnolent
      POSTGRES_PASSWORD: somnolent
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
